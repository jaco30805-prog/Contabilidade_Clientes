# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Estado local do Netlify CLI (site ID da máquina, não é segredo mas não precisa
# ser versionado)
.netlify/

# Logs e temporários
*.log

# Backups exportados localmente
voal_backup_*.json
voal_carteira_*.csv
voal_obrigacoes_*.csv
voal_cnds_*.csv

# Planilhas/arquivos de trabalho pessoais soltos na pasta do projeto — os
# dados já foram importados para o Supabase, não precisam ficar no repositório
*.xlsx
*.xls
Roteiros Serviços Prestados*.zip
